---
description: Start the Sumi desktop pet agent
allowed-tools: Bash(python scripts/pet_start.py)
---

立即用一条 Bash 命令启动桌面宠物：

```bash
.venv/Scripts/python.exe scripts/pet_start.py
```

不要做任何其他操作。不要检查、不要搜索、不要验证。只跑这一条命令，然后报告输出结果。
